{
  "name": "flaremo-memory",
  "version": "0.1.0",
  "description": "FlareMo 记忆账本接入：开工查规矩、工作中记事实、收工战报；含 MCP 端点接线与生命周期 Hook 自动化。",
  "description_i18n": {
    "en": "FlareMo memory ledger: recall rules at session start, record facts while working, checkpoint at session end; wires the memory MCP endpoint and lifecycle hooks.",
    "zh-CN": "FlareMo 记忆账本：开工查规矩、工作中记事实、收工战报；含 MCP 端点接线与生命周期 Hook 自动化。"
  },
  "author": {
    "name": "FlareMo"
  },
  "license": "AGPL-3.0-only",
  "skills": "skills",
  "commands": "commands",
  "hooks": "hooks",
  "mcpServers": {
    "flaremo-memory": {
      "type": "http",
      "url": "${user_config.url}/memory/mcp",
      "headers": [
        {
          "name": "Authorization",
          "value": "Bearer ${user_config.pat}"
        }
      ]
    }
  },
  "user_config": {
    "url": {
      "type": "string",
      "title": "FlareMo 实例 URL",
      "title_i18n": { "en": "FlareMo instance URL", "zh-CN": "FlareMo 实例 URL" },
      "description": "例如 https://flaremo.example.com",
      "required": true
    },
    "pat": {
      "type": "string",
      "title": "访问令牌 (PAT)",
      "title_i18n": { "en": "Personal access token (PAT)", "zh-CN": "访问令牌 (PAT)" },
      "description": "在 FlareMo Web → 设置 → 个人访问令牌 创建",
      "required": true,
      "sensitive": true
    }
  }
}